# Development-only dependencies (not needed for Streamlit Cloud deployment)
# Install with: pip install -r requirements-dev.txt

-r requirements.txt

# Dataset download
kagglehub[pandas-datasets]>=1.0.0
openpyxl>=3.1.0

# Testing
pytest>=8.0.0
